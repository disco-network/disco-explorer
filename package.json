{
  "name": "disco-explorer",
  "version": "0.0.1",
  "description": "An admin ui for managing a disco-node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server",
    "postinstall": "node_modules/typescript/bin/tsc -p ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lite-server": "^2.2.2",
    "typescript": "^2.0.10"
  }
}
